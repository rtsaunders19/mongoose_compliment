/*require('dotenv').config();

var mongoose = require('mongoose');

mongoose.connect(process.env.COMP_DB_CONN);

var Compliments = require('./models/compliment');



Compliment.create(compliments, function (err, compliments) {
  if (err) {
    console.log(err)
  }


})
*/
